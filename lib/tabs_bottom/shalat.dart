import 'package:flutter/material.dart';

class shalat extends StatelessWidget {
  const shalat({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: const Text('Dalam Tahap Pengembangan'),
        ),
      ),
    );
  }
}
